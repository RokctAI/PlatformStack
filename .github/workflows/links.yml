name: Link Checker
env:
  PIP_BREAK_SYSTEM_PACKAGES: 1
on:
  schedule:
    - cron: '30 2 * * 0' # Weekly on Sunday
  workflow_dispatch:

jobs:
  links:
    uses: RokctAI/shared-workflows/.github/workflows/universal-links.yml@2c7f49674dc9c14ca390f3342ad514cff59f64f5 # main
    secrets: inherit
