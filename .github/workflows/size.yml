name: Size Labeler
env:
  PIP_BREAK_SYSTEM_PACKAGES: 1
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  size:
    uses: RokctAI/shared-workflows/.github/workflows/universal-size.yml@09494edd4824cc6fca95ea96a2db4d238c4561b3 # shared-workflows main @ 2026-08-26
    permissions: write-all
    secrets: inherit
