name: Semantic PR
env:
  PIP_BREAK_SYSTEM_PACKAGES: 1
on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  semantic:
    uses: RokctAI/shared-workflows/.github/workflows/universal-semantic.yml@1d5ebbcdf3dc771fed587fe90e5f91f3a305392c # shared-workflows main @ 2026-08-24
    permissions: write-all
    secrets: inherit
