name: Security Scan
env:
  PIP_BREAK_SYSTEM_PACKAGES: 1
on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    permissions: write-all
    uses: RokctAI/shared-workflows/.github/workflows/universal-security.yml@0baa82d350e5ad1bc8d3950c1417c320fd347c7b # main
    secrets: inherit

