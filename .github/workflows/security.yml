name: Security Scan
env:
  PIP_BREAK_SYSTEM_PACKAGES: 1
on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    permissions: write-all
    uses: RokctAI/shared-workflows/.github/workflows/universal-security.yml@35c8f0677c1b2b3b73d3c6883bf102f192ca2bcd # main
    secrets: inherit

